global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - "alerts.yml"

scrape_configs:
  - job_name: "goldpay-api"
    metrics_path: "/metrics"
    static_configs:
      - targets: ["api:3000"]
        labels:
          environment: "production"
          service: "goldpay-gateway"
